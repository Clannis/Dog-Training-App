Name: <%= text_field_tag "[course][name]", @course.name %><br>
Length: <%= select_tag "[course][length]", options_for_select((1..20), @course.length)%> weeks<br>
Cost: $<%= text_field_tag "[course][cost]", @course.cost %><br>
<%= submit_tag "Submit" %>